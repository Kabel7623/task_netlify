# azure-pipelines.yml

trigger:
  - main  # or use '*' to run for all branches

pool:
  vmImage: 'ubuntu-latest'

steps:
  - task: UseNode@1
    inputs:
      version: '18.x'  # change based on your project requirements

